{-# OPTIONS_GHC -Wno-unrecognised-pragmas #-}
{-# HLINT ignore "Use foldr" #-}

module Sandbox where

sum' = undefined

concat' = undefined

length' = undefined

elem' = undefined

zip' = undefined

last' = undefined

init' = undefined